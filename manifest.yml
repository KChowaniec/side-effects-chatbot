---
declared-services:
  conversation-demo:
    label: conversation
    plan: free
applications:
- name: AdverseSideEffectsChatbot
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - conversation-demo
  env:
    NPM_CONFIG_PRODUCTION: false